interface:
  display_name: "Iterative Plan Review"
  short_description: "Draft, stress-test, and persist execution plans"
  default_prompt: "Use $iterative-plan-review to turn this task into an execution plan, review it in up to three subagent passes, persist each pass into compact JSON artifacts, and bring me only the business decisions that need my input."
