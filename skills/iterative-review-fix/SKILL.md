---
name: iterative-review-fix
description: Закрывать цикл review findings (замечаний ревью) от разбора до исправления, верификации и повторного ревью через до трёх независимых проходов сильным субагентом, сохраняя полный след артефактов и поднимая пользователю только те вопросы, которые требуют бизнес-решения. Использовать, когда пользователь приносит code review (ревью кода), PR comments (комментарии к PR), список findings (замечаний), просит исправить замечания, довести change set (набор изменений) до merge-ready (готовности к вливанию) состояния или хочет управляемый цикл review, fix и re-review.
---

# Iterative Review Fix

## Overview

Превращать набор замечаний в управляемый контур исправления, а не в хаотичную серию точечных правок.

Держать ответственность разделённой:

- агент отвечает за проверку замечаний по фактам, порядок исправлений, реализацию, качество и повторное ревью;
- пользователь отвечает за product choices (продуктовые выборы), допустимые компромиссы поведения, scope (границы) исправлений и осознанные отложенные решения.

Сохранять весь след работы как артефакты, чтобы было видно, какое замечание подтвердилось, как именно оно было закрыто и почему цикл остановился.

## Artifact Workspace

Создавать отдельную директорию под каждый прогон навыка.

По умолчанию хранить артефакты в текущем work root (рабочем корне) по пути:

```text
.codex-artifacts/iterative-review-fix/sessions/<timestamp>-<task-slug>/
```

Для инициализации использовать скрипт:

```bash
python3 "${CODEX_HOME:-$HOME/.codex}/skills/iterative-review-fix/scripts/init_review_fix_run.py" \
  --workspace-root <ABSOLUTE_WORK_ROOT> \
  --task "<RAW_USER_TASK>" \
  --slug "<SHORT_ASCII_SLUG>"
```

`--slug` задавать как короткое читаемое имя задачи в ASCII, если агент уже понимает бизнес-смысл задачи. Если `--slug` не передан, скрипт сам создаёт безопасное имя папки.

Скрипт создаёт:

- `00-input/` для исходной задачи, списка замечаний, границ и проектного контекста;
- `01-analysis/` для разбора замечаний и стратегии исправления;
- `02-working/` для текущего статуса и плана правок;
- `03-history/` для истории общения, решений и журнала изменений;
- `04-reviews/iteration-N/` для каждого прохода re-review (повторного ревью);
- `05-final/` для финального статуса замечаний и сводки проверки;
- `manifest.json` как машинно-читаемую карту сессии.

Структуру и назначение файлов смотреть в [references/artifact-layout.md](references/artifact-layout.md).

`02-working/current-status.md` считать SSOT текущего состояния review-fix цикла.

## Workflow

1. Собирать контекст до первого исправления.
- Прочитать локальный `AGENTS.md` и ближайшие проектные правила, если работа идёт внутри репозитория.
- Найти исходные review findings (замечания), связанный diff (дифф), ветку, код, тесты, миграции и затронутые контракты.
- Сразу после старта создать artifact workspace (рабочее пространство артефактов) и сохранить исходную задачу в `00-input/task.md`.
- Сохранить сырой список замечаний в `00-input/review-findings.md`.
- Сохранить подтверждённые ограничения и границы в `00-input/scope-and-constraints.md`.

2. Разобрать замечания по фактам.
- Для каждого замечания определить: подтверждается ли оно, частично ли подтверждается, уже ли исправлено, или требует уточнения.
- Не исправлять вслепую только по формулировке ревью; сначала сверять замечание с кодом и локальными правилами.
- Сохранять разбор в `01-analysis/findings-analysis.md`.
- Формировать порядок работы по риску и зависимости, а не по случайному порядку списка.
- Сохранять стратегию исправления в `01-analysis/fix-strategy.md`.

3. Отделять рабочие исправления от бизнес-развилок.
- Считать бизнес-развилкой изменение, которое меняет поведение продукта, внешний контракт, допустимый компромисс качества, совместимость или состав поставки.
- Считать рабочим исправлением перестройку кода, тестов, структуры, проверок, логирования и порядка операций, если бизнес-смысл уже определён.
- Поднимать бизнес-развилки пользователю вопросом и сохранять их в `01-analysis/questions-for-user.md`.
- Закрывать рабочие исправления самостоятельно.

4. Выполнять fix batch (пакет исправлений).
- Сначала убирать подтверждённые дефекты, которые дают максимальный риск для поведения, архитектуры и качества.
- Следовать локальным правилам по инспекциям, quality gate (контролю качества) и тестам.
- Если проект требует сначала исправить inspections (инспекции), делать это до quality и тестов.
- Обновлять `02-working/implementation-plan.md`, `02-working/current-status.md` и `02-working/changed-surface.md`.

5. Проводить от одного до трёх проходов re-review.
- Всегда проводить хотя бы один проход повторного ревью после meaningful fix batch (содержательного пакета исправлений).
- На каждой итерации запускать нового сильного субагента.
- Всегда использовать `fork_context=false`.
- Предпочитать сильную модель уровня `gpt-5.4` с `reasoning_effort=high`, если она доступна.
- Передавать субагенту только локально нужный контекст: подтверждённые замечания, что уже исправлено, текущий diff, результаты верификации и открытые вопросы.
- Не передавать субагенту желаемый итог, свой диагноз или подсказку о том, что именно он должен “одобрить”.
- Требовать от субагента проводить re-review самостоятельно, без запуска дочерних субагентов, `spawn_agent`, `codex-subagent-exec`, `codex exec` или любых других механизмов делегации.

6. После каждой итерации перерабатывать решение.
- Разделять замечания субагента на:
  - остаточные дефекты;
  - недостающие проверки;
  - бизнес-вопросы к пользователю;
  - несущественный шум, который не меняет решение.
- До отправки субагенту сохранять текущий срез в `04-reviews/iteration-N/findings-in-scope.md`, `fixes-applied.md`, `verification.md` и `subagent-review-prompt.md`.
- Сохранять ответ субагента в `04-reviews/iteration-N/subagent-review.md`.
- Сохранять вопросы пользователю в `04-reviews/iteration-N/questions-for-user.md`.
- Сохранять ответы пользователя в `04-reviews/iteration-N/user-answers.md`.
- Сохранять решение о продолжении или остановке цикла в `04-reviews/iteration-N/exit-decision.md`.
- Перезаписывать `02-working/current-status.md` и добавлять краткую запись в `03-history/change-log.md`.

7. Возвращать финальный результат.
- Показывать, какие замечания закрыты, какие остались и почему.
- Отдельно перечислять бизнес-решения, подтверждённые пользователем по ходу цикла.
- Отдельно помечать остаточный риск и открытые вопросы.
- Записывать итог в `05-final/resolved-findings.md`, `05-final/remaining-findings.md`, `05-final/final-summary.md` и `05-final/verification-summary.md`.

## Up-To-Three Re-Review Loop

Для каждой итерации использовать один и тот же каркас:

1. Зафиксировать, какие findings (замечания) входят в текущий проход.
2. Внести и верифицировать исправления.
3. Передать текущий результат свежему субагенту на критическое re-review.
4. Разобрать замечания субагента.
5. Поднять бизнес-вопросы пользователю, если они появились.
6. Обновить решение и оценить, нужен ли следующий проход.

Во время каждого прохода поддерживать history trail (исторический след):

- добавлять в `03-history/conversation-log.md` вопросы агенту и пользователю в хронологическом порядке;
- добавлять в `03-history/decision-log.md` только решения, которые пользователь подтвердил явно;
- не стирать старые формулировки из исторических файлов, если более поздняя итерация изменила трактовку замечания или способ исправления.

Останавливать цикл раньше третьего прохода, если одновременно выполняются все условия:

- последний проход не нашёл material issues (существенных проблем), которые требуют новых кодовых изменений;
- у субагента не появилось новых business questions (бизнес-вопросов) к пользователю;
- текущий контур прошёл обязательные inspections (инспекции), quality checks (проверки качества) и нужные тесты по локальным правилам;
- оставшиеся замечания либо закрыты, либо явно переведены в осознанный остаточный риск с объяснением причины.

Запускать второй или третий проход, если хотя бы одно из условий не выполнено.

## Subagent Contract

Субагенту давать роль критика остаточного риска, а не автора бизнес-решения.

Требовать от него:

- проверять, какие findings реально закрыты, а какие замаскированы частичной правкой;
- искать регрессии, architectural drift (архитектурный дрейф), пропущенные проверки и слишком узкие фиксы;
- отдельно формулировать вопросы, которые требуют решения пользователя;
- не выбирать за пользователя поведенческий компромисс, scope фикса или допустимый технический долг;
- проводить анализ самостоятельно внутри своей сессии, без каскадной делегации и без запуска дочерних субагентов любым способом;
- возвращать вывод компактно и без воды.

Если субагент начинает принимать бизнес-решения сам, не переносить такие выводы в решение как факт. Переформулировать их в вопрос к пользователю.

## Verification Rules

Считать верификацию обязательной частью каждого meaningful fix batch (содержательного пакета исправлений).

- Сначала запускать inspections (инспекции) по изменённому контуру, если этого требуют локальные правила.
- Затем запускать quality gate (контроль качества) по изменённому контуру.
- Затем запускать тесты в том объёме, который требует проект.
- Если локальные правила требуют полный прогон тестов в конце задачи, выполнять его до финализации.
- Сохранять результаты и краткий смысл проверок в `04-reviews/iteration-N/verification.md` и затем в `05-final/verification-summary.md`.

## Prompt Templates

### Main Agent Prompt Shape

Использовать такой каркас мышления:

```text
Задача пользователя: {task}

Подтвержденный контекст:
- ...

Исходные замечания:
- ...

Подтвержденные бизнес-решения:
- ...

Открытые вопросы:
- ...

Текущий статус исправлений:
- ...
```

### Subagent Prompt

Использовать короткий рабочий промпт такого вида:

```text
Проведи критическое re-review текущего набора исправлений.

Работай самостоятельно в рамках своей сессии.
Не запускай других субагентов, `spawn_agent`, `codex-subagent-exec`, `codex exec` и любые другие механизмы делегации.

Контекст задачи:
{task_summary}

Подтвержденные замечания:
- ...

Что уже исправлено:
- ...

Что проверено:
- ...

Открытые вопросы:
- ...

Сделай четыре вещи:
1. Найди замечания, которые ещё не закрыты или закрыты только частично.
2. Найди регрессии, архитектурные нарушения и недостающие проверки.
3. Отдельно выдели вопросы, которые меняют бизнес-решение и требуют ответа пользователя.
4. Предложи следующий fix batch без самостоятельного выбора бизнес-варианта.

Формат ответа:
- Remaining issues
- Regression risks
- Questions for user
- Next fix batch
```

## Decision Boundary

Поднимать пользователю вопрос, если выбор относится к одному из случаев:

- менять ли поведение продукта или только внутреннюю реализацию;
- ужесточать ли внешний контракт, совместимость или правила валидации;
- какой компромисс допустим между скоростью исправления и шириной охвата;
- нужно ли закрывать замечание полностью сейчас или осознанно переносить его;
- какой остаточный риск допустим для этой поставки.

Закрывать самостоятельно, если вопрос относится к одному из случаев:

- как разложить исправления по пакетам;
- в каком порядке исправлять подтверждённые дефекты;
- как привести код к локальным архитектурным правилам;
- какие тесты, inspections и quality checks нужны для верификации;
- как оформить артефакты и итоговый статус замечаний.

## Output Format

Итоговый ответ строить в таком порядке:

1. Коротко сформулировать, что и зачем исправлено.
2. Показать статус замечаний: закрыто, частично, осталось.
3. Показать подтверждённые пользователем бизнес-решения.
4. Показать остаточный риск и открытые вопросы, если они ещё влияют на выпуск.

Если пользователь просит только закрыть замечания без подробного отчёта, оставлять основным результатом статус замечаний и короткий блок с рисками.

## Rules

- Начинать с контекста и проверки замечаний по фактам.
- Всегда создавать и вести artifact workspace для каждого нового прогона навыка.
- Не исправлять ревью вслепую только по тексту комментария.
- Задавать вопросы по ходу работы, а не копить их до самого конца.
- Проводить от одного до трёх независимых проходов re-review разными свежими субагентами.
- Субагент, запущенный в рамках этого skill, проводит re-review сам и не запускает других субагентов никаким способом.
- Внутри субагента запрещены `spawn_agent`, `codex-subagent-exec`, `codex exec`, `codex e` и любые иные механизмы каскадной делегации.
- Не использовать этот skill рекурсивно внутри субагента.
- Не выдавать предположения за подтверждённые факты.
- Не принимать бизнес-решения за пользователя.
- Хранить историю append-only (только дописывание), кроме `02-working/current-status.md`, который всегда отражает текущую рабочую версию цикла.

## Trigger Examples

- `Используй $iterative-review-fix и закрой эти замечания ревью с повторной проверкой результата.`
- `Разбери review findings, исправь подтверждённые проблемы и прогони re-review до устойчивого состояния.`
- `Доведи этот change set до merge-ready через цикл review -> fix -> re-review, а спорные бизнес-моменты сначала согласуй со мной.`
