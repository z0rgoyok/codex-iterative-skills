interface:
  display_name: "Iterative Review Fix"
  short_description: "Resolve review findings with compact JSON artifacts"
  default_prompt: "Use $iterative-review-fix to turn these review findings into fixes, keep a compact machine-parsable artifact trail, and re-review the result up to three times while escalating business decisions to me."
